# Contracts

Canonical machine- and human-readable public contracts.

## Intended contents

- OpenAPI specs
- JSON schema definitions
- Versioned contract snapshots for major releases

MVP currently uses `docs/API-Contract.md` as the canonical contract reference.
