# Project Status - Environment Configuration
# Copy this file to .env.local and fill in actual values for local development.
# Never commit .env files with secrets to git.

# API Configuration for web client
VITE_API_BASE_URL=http://localhost:5000

# CLI Configuration (optional - can also use `config` command)
# PROJECT_STATUS_API_URL=http://localhost:5000
# PROJECT_STATUS_OUTPUT=table
